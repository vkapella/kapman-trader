{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://kapman-trader/schemas/dealer_metrics_config.schema.json",
  "title": "Dealer Metrics Config",
  "description": "Structural contract for dealer/options aggregation configuration without metric calculations.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "aggregation_definition": {
      "type": "object",
      "description": "Definition of the dealer metrics aggregation method.",
      "properties": {
        "method": {
          "type": "string",
          "description": "Name of the aggregation method."},
        "parameters": {
          "type": "object",
          "description": "Opaque parameters for the aggregation method."}
      },
      "required": ["method", "parameters"],
      "additionalProperties": false
    }
  },
  "required": ["aggregation_definition"]
}
