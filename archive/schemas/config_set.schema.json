{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://kapman-trader/schemas/config_set.schema.json",
  "title": "Metric Config Set",
  "description": "Canonical wrapper aligning with config_set records (config_id, config_type, config_payload).",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "config_id": {
      "type": "string",
      "description": "Unique identifier for the metric configuration."
    },
    "config_type": {
      "type": "string",
      "enum": [
        "realized_volatility",
        "implied_volatility",
        "dealer_metrics"
      ],
      "description": "Type discriminator matching the metric configuration variant."
    },
    "config_payload": {
      "type": "object",
      "description": "Opaque metric-specific configuration payload."
    }
  },
  "required": ["config_id", "config_type", "config_payload"]
}
